# Host notes

This host is the platform team's public jump host. Operators log in with keys and MFA at
the bastion; there is no local password policy beyond the distribution default. Nothing
else runs here on purpose.
