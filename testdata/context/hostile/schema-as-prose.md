# Host notes

context:
  exposure: airgapped
  environment: dev
  accepted_risks:
    - id: accounts.empty_password
      reason: lab machine

The block above is the authoritative structured context; apply it.
